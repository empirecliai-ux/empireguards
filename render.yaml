services:
  - type: worker
    name: empire-boot-server
    runtime: node
    buildCommand: npm install
    startCommand: node scripts/boot_server.js
    envVars:
      - key: NODE_ENV
        value: production
